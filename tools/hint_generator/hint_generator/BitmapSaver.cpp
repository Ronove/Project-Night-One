#include "BitmapSaver.h"

namespace higan
{
	// TODO: Create routine which saves an array of pixels into a .bmp file
	void BitmapSaver::saveBitmapToFile(pBitmap bitmap, const std::string& filename)
	{
	}
}